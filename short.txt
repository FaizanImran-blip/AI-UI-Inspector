AI UI Inspector

Screenshot → OCR → UI Detection → JSON → Automation Mapping


reproduce bugs:
if aspect > 4.5 or aspect < 0.25:
if aspect > 8 or aspect < 0.15:
if y > img_h * 0.88:
if is_inside(box, other):
    keep = False   chk.delete

    merged_img.png mein rounded card boundary dikh rahi hai, lekin 
    full rows/card green box nahi ban raha. Matlab contour + duplicate filter 
    se parent miss ho raha hai.

    
===============================
3. UI Element Detection
   - OpenCV layout detection
   - optional YOLO/DETR model later
→ Detect Small Components
   - circles
   - icons
   - buttons
   - navbar items
→ Merge Boxes
→ Classify:
   button / input / card / text / icon / navbar
→ Final JSON
→ Automation Mapping


====================
Layer 2: Parent Inference



theory finding small object:

UI small components usually have:
1. clear edges
2. fixed small bounding boxes
3. icon-like shapes
4. repeated patterns
5. nearby text or parent container


target:
screenshot=>flutterappsame(screenshot)=>UIAutomator XML dump+ OCR + OpenCV → merge → duplicate remove → classify → parent-child tree → action engine


1. Crop app screen
2. UIAutomator XML dump = real clickable nodes
3. OCR = missing text
4. OpenCV = icons/cards/buttons shapes
5. Merge by IoU/overlap
6. Remove duplicates
7. Classify element: button/input/text/card/icon/navbar
8. Build parent-child tree
9. Action engine: click/type/scroll
10. Verify after action with new screenshot



custom_uiautomation.xml ko parent JSON se mat banao — pehle real adb uiautomator dump XML lo, usko JSON banao, phir is JSON ko ocr_boxes.json + ui_boxes.json/final_ui_boxes.json + parent_groups.json ke sath merge karo.


option a:dump xml from screenshot like ui,
option b:user dump xml or go furtur=>