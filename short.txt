Flutter app screenshot
↓
ADB / Appium / uiautomator dump
↓
real XML hierarchy
↓
OCR/OpenCV only fallback
↓
automation action. next architecture,




done:assets/ui.png
   ↓
indent.py
   ↓
image preprocessing
   ↓
OCR text + OCR boxes
   ↓
OpenCV rough UI boxes
   ↓
final_ui_boxes.json
   ↓
parent.py parent_grouping()
   ↓
parent_groups.json + parents.png
   ↓
ui.py
   ↓
custom UI XML


current pipeline:

Screenshot
→ OCR
→ OpenCV rough boxes
→ remove duplicate boxes
→ parent grouping
→ JSON/XML output