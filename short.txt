Flutter app screenshot
↓
ADB / Appium / uiautomator dump
↓
real XML hierarchy
↓
OCR/OpenCV only fallback
↓
automation action. next architecture,




done:assets/ui.png
   ↓
indent.py
   ↓
image preprocessing
   ↓
OCR text + OCR boxes
   ↓
OpenCV rough UI boxes
   ↓
final_ui_boxes.json
   ↓
parent.py parent_grouping()
   ↓
parent_groups.json + parents.png
   ↓
ui.py
   ↓
custom UI XML
flow:
ui.xml
 ↓
inspector_output.json
 ↓
draw_boxes.py
 ↓
inspector.png
======================
current pipeline:XML dump → XML parser → screenshot par XML boxes draw → inspector JSON → automation action