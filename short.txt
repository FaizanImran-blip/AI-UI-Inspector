AI UI Inspector

Screenshot → OCR → UI Detection → JSON → Automation Mapping



===============================
3. UI Element Detection
   - OpenCV layout detection
   - optional YOLO/DETR model later
→ Detect Small Components
   - circles
   - icons
   - buttons
   - navbar items
→ Merge Boxes
→ Classify:
   button / input / card / text / icon / navbar
→ Final JSON
→ Automation Mapping


theory finding small object:

UI small components usually have:
1. clear edges
2. fixed small bounding boxes
3. icon-like shapes
4. repeated patterns
5. nearby text or parent container
















today:
Final Clean Boxes
→ Parent Grouping
→ Classification
→ Structured UI JSON
→ Automation Mapping



{
   flow:Dono files mil kar ye architecture chala rahi hain:

main file / indent.py
assets/ui.png
→ image load + size
→ OCR preprocessing
→ OCR text extract
→ OCR boxes extract
→ OCR boxes scale down
→ OpenCV large boxes detect
→ OpenCV small components detect
→ text-overlap small boxes remove
→ medium_grouping() call from parent.py
→ final_detection.png + final_ui_boxes.json save
→ parent_grouping() call from parent.py

Phir parent.py:

final_ui_boxes.json
+ ocr_boxes_scaled.json
→ parent candidates bante hain
→ har parent ke andar OCR/text/UI children check hote hain
→ parent_groups.json save hota hai
→ parents.png draw hoti hai
}